@inject NavigationManager navigationManager

@code {
    // I'm not so sure about the correct way of doing this but this probably isn't it
    protected override void OnInitialized()
    {
        navigationManager.NavigateTo("/");
    }
}
