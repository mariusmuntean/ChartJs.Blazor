@inject NavigationManager navigationManager

@code {
    // I'm not so sure about the correct way of doing this
    protected override void OnInitialized()
    {
        navigationManager.NavigateTo("/");
    }
}
